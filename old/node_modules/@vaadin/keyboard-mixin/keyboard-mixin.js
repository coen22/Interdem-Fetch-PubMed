import { applyMixin, wasApplied } from '@vaadin/mixin-utils';
export function KeyboardMixin(base) {
    if (wasApplied(KeyboardMixin, base)) {
        return base;
    }
    class Keyboard extends base {
        firstUpdated(props) {
            super.firstUpdated(props);
            this.addEventListener('keydown', (event) => {
                this._onKeyDown(event);
            });
            this.addEventListener('keyup', (event) => {
                this._onKeyUp(event);
            });
        }
        _onKeyDown(_event) {
            // to be implemented
        }
        _onKeyUp(_event) {
            // to be implemented
        }
    }
    applyMixin(KeyboardMixin, Keyboard);
    return Keyboard;
}
//# sourceMappingURL=keyboard-mixin.js.map